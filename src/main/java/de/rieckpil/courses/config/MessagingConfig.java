package de.rieckpil.courses.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MessagingConfig {

  // No longer required as overriding the AWS SDK clients works now via a property
}
